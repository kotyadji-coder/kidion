# Legacy Curricula

These JSON files are older short-form curriculum templates kept for reference.
They are not loaded by the active product path.

Active Kidion curriculum data lives in `data/curricula/routes/` and is loaded
through `services.curriculum_routes`.
